# ============================================
# PRODUCCIÓN - CuantoSabe
# ============================================

# Application
APP_NAME="Juego Cuanto Sabe"
APP_ENV=production
APP_KEY=base64:3vuI/Jx//H2jNJ7VF2eRdcXCbATCx13C+E8+REDSxqw=
APP_DEBUG=false
APP_URL=https://app.cuantosabe.com.ar

APP_LOCALE=es
APP_FALLBACK_LOCALE=es
APP_FAKER_LOCALE=es_AR

APP_MAINTENANCE_DRIVER=file
BCRYPT_ROUNDS=12

# Logging
LOG_CHANNEL=stack
LOG_STACK=daily
LOG_DEPRECATIONS_CHANNEL=null
LOG_LEVEL=error

# Database
DB_CONNECTION=mysql
DB_HOST=db
DB_PORT=3306
DB_DATABASE=cuanto_sabe_db
DB_USERNAME=CuantoSabe
DB_PASSWORD=CuantoSabe2025!
# ⚠️ IMPORTANTE: Cambia esta password en producción

# Session (usando database en producción)
SESSION_DRIVER=file
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=.cuantosabe.com.ar
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

# Broadcasting
BROADCAST_CONNECTION=pusher
BROADCAST_DRIVER=pusher

# Pusher Configuration
PUSHER_APP_ID=2019775
PUSHER_APP_KEY=b6ac4842db4cad2e2c14
PUSHER_APP_SECRET=726404c507a2de730933
PUSHER_APP_CLUSTER=sa1
PUSHER_PORT=443
PUSHER_SCHEME=https

# Filesystem
FILESYSTEM_DISK=local

# Queue (usando database en producción)
QUEUE_CONNECTION=database

# Cache (usando database en producción)
CACHE_STORE=database
CACHE_DRIVER=database

# Redis (no lo usas por ahora, pero está disponible)
REDIS_CLIENT=phpredis
REDIS_HOST=redis
REDIS_PASSWORD=null
REDIS_PORT=6379

# Mail (configurar cuando lo necesites)
MAIL_MAILER=log
MAIL_HOST=127.0.0.1
MAIL_PORT=2525
MAIL_USERNAME=null
MAIL_PASSWORD=null
MAIL_ENCRYPTION=null
MAIL_FROM_ADDRESS="noreply@cuantosabe.com.ar"
MAIL_FROM_NAME="${APP_NAME}"

# Vite (Frontend)
VITE_APP_NAME="${APP_NAME}"
VITE_PUSHER_APP_KEY="${PUSHER_APP_KEY}"
VITE_PUSHER_APP_CLUSTER="${PUSHER_APP_CLUSTER}"
VITE_PUSHER_HOST=
VITE_PUSHER_PORT="${PUSHER_PORT}"
VITE_PUSHER_SCHEME="${PUSHER_SCHEME}"